export function getEnvExample(envLines: string[]): string {
  return `${envLines.join("\n").trimEnd()}\n`;
}

export function getEnvFile(envLines: string[]): string {
  return `${envLines.join("\n").trimEnd()}\n`;
}

export function getEnvLocal(envLines: string[]): string {
  return `${envLines.join("\n").trimEnd()}\n`;
}

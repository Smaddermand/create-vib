export function getEnvExample(envLines) {
  return `${envLines.join("\n").trimEnd()}\n`;
}

export function getEnvFile(envLines) {
  return `${envLines.join("\n").trimEnd()}\n`;
}

export function getEnvLocal(envLines) {
  return `${envLines.join("\n").trimEnd()}\n`;
}
